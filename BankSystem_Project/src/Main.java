
public class Main {
    public static void main(String[] args) {
        
           
           
    	BankSys.readTxt("customerInfo.txt");

        // Müşteri oluşturma
          //BankSys.addAcc(1001,  0, customer, 1000);

           //customer.getInput();
           //account.getInput1();
        // Banka hesabı oluşturma
        //bankSystem.addAcc(1001, 1, bankSystem.getCustomer(0), 1000.0);

        // Hesapları görüntüleme
          BankSys.displayAcc();

        // Hesap sahibi olan müşteriye para yatırma
         //account = BankSys.searchAccount("1001");
       
        /*if (account != null) {
            account.deposit(500.0);
        }

        // Hesap sahibi olan müşteriden para çekme
        if (account != null) {
            account.withdraw(200.0);
        }

       */
        

        // Hesap silme
         //BankSys.removeAcc("1001");
        
         //BankSys.displayAcc();

        
    }
}

